# 「运营数据管理」项目当前进度报告

**报告日期：** 2026-07-07
**报告人：** Claude（对话 20260707001）
**项目位置：** `00_工作台/运营数据管理/`

---

## 一、整体进展（截止 2026-07-07）

### 已完成（10 期迭代）

| 阶段 | 状态 |
|------|------|
| MVP 第一期：POS 收银机 | ✅ 完成 |
| MVP 第二期：产品动态管理 | ✅ 完成 |
| 用户登录系统（三角色 admin/editor/viewer） | ✅ 完成 |
| 文创产品管理模块（CRUD + 表格导入 + 库存联动） | ✅ 完成 |
| 腾讯云 Docker 部署（Nginx + Node.js API + PostgreSQL 17） | ✅ 完成 |
| 数据从 Supabase 迁移至腾讯云（165+ 条 revenue） | ✅ 完成 |
| Git 版本管理 | ✅ 完成 |
| 操作日志审计 | ✅ 完成 |
| **第十期**录入 bug 修复 + 统计可视化扩展（**最新**） | ✅ 完成 |

### 最新一期（第十期，2026-07-07）核心成果

1. **套票录入 bug 修复** — 录入 4 张套票时不再错误生成 4 张普通票
2. **收银台顶部实时统计扩展** — 由 2 项扩展为 7 项分项（门票/套票/咖啡/工坊/文创/其他）+ 合计
3. **收入趋势图表补「其他」分类** — 日趋势/月度趋势均补全 5 处
4. **侧边栏版本号修复** — `__APP_VERSION__` 占位符已替换为实际 `v1.1.0`

### 当前线上状态

- **访问地址**：http://122.51.56.50（正常响应）
- **数据库**：revenue 175+ 条，今日（7-7）有 3 条新记录（其他 16.5/2/文创 15），运营活跃
- **版本**：v1.1.0
- **未提交变更**：`+177 -18`，主要是 build-version.js 自动化脚本（注入 APP_VERSION + 更新日期）

---

## 二、待办事项（汇总自开发日志）

### A. P0 立即可做（业务急迫）

| 任务 | 来源 | 状态 | 优先级 |
|------|------|------|--------|
| ~~场地租金独立录入标签页~~ | 第五/六期 | ✅ **已完成**（功能在「空间使用」Tab 内）| — |
| ~~画廊销售独立录入标签页~~ | 第五/六期 | ✅ **已完成**（「🖼️ 画廊销售」Tab 独立入口）| — |
| POS 收款后文创库存自动扣减 | 第九期 | 📋 待启动 | ⭐⭐⭐ |
| 退款/作废功能 | 第五/六期 | 📋 待启动 | ⭐⭐⭐ |
| 采购入库记录（进货批次管理） | 第九期 | 📋 待启动 | ⭐⭐ |
| 日结/交班报表 | 第五期 | 📋 待启动 | ⭐⭐ |

> **2026-07-07 复盘修正**：「场地租金独立录入标签页」这一项 P0 任务**实际早已完成**。当前侧边栏「🏛 空间使用」Tab 就是该功能：
> - 数据库表 `space_usage` 字段含 `rental_type` / `receivable_amount` / `received_amount`，见 [app/sql/init.sql:52-67](app/sql/init.sql)
> - 录入表单 + 空间看板 + 时间冲突检测 + 月度筛选 + CSV 导出，全部就位
> - 收银台首页显示「⚠️ 场地租金待收款」提示（[app/js/ui.js:803-813](app/js/ui.js)）
> - 当月总收入已包含 `rentalType === '付费'` 的 `receivedAmount`
>
> 第五/六期日志之所以仍把这项标为待办，是因为当时还没合并到空间使用 Tab 完成；后来迭代已经实现但日志没回填。后续维护：日志/报告之间要双向同步。

#### 2026-07-07 复盘 P0 全量盘点（继续发现）

既然发现日志/报告失同步会误导决策，对剩余 5 项 P0 逐个做了代码核对：

| 任务 | 状态 | 证据 |
|------|------|------|
| POS 收款 → 文创库存自动扣减 | 📋 **未完成** | `ui.js:365-375` 只在选产品时过滤库存，没有收款后扣减逻辑；`stock` 字段在产品 CRUD 可见但收款路径不写回 |
| 退款/作废功能 | 📋 **未完成** | `ui.js` 全文件无 `refund` / `void` 关键词命中，revenue 表无 `status` 字段，仅有 delete |
| 画廊销售独立录入标签页 | ✅ **已完成** | `ui.js:1297 renderGalleryPage` + index.html:45 导航 `🖼️ 画廊销售` + DB 表 `gallery_sales` + 导出 + 卡片统计全部就位（与场地租金相同模式，被日志遗漏）|
| 采购入库记录 | 📋 **未完成** | 全仓无 `purchase` / `stockIn` / `inbound` / `进货批次` 命中 |
| 日结/交班报表 | 📋 **未完成** | 全仓无 `日结` / `交班` / `settlement` 命中 |

**结论**：原 5 项 P0 中「画廊销售独立 Tab」也早已完成（与场地租金 Tab 同期实现），需要从待办移到已完成。剩余真实 P0 = 4 项：**文创库存扣减 / 退款作废 / 采购入库 / 日结报表**。

### B. P1 增强功能

| 任务 | 说明 |
|------|------|
| 收银交易记录支持打印小票 | POS 标配 |
| 历史数据对比分析（同比/环比） | 数据洞察 |
| 库存预警（低于安全库存提示） | 配套采购 |
| 供应商管理模块（联系方式/账期） | 配套采购 |
| 细粒度操作日志审计 | 已做基础，可深化 |

### C. 技术债（开发计划已列）

| 风险 | 说明 | 建议处理 |
|------|------|----------|
| `ui.js` 2471 行单文件 | 维护成本上升 | 拆分 POS/管理/统计模块 |
| 无 HTTPS | HTTP 环境下 `crypto.subtle` 不可用 | 服务端登录已绕过，长期需上 HTTPS |
| API 无认证中间件 | 可被直接调用 | 加 JWT/session 中间件 |
| 无数据库自动备份 | 仅手动 `pg_dump` | 加 cron + 备份到 OSS |

---

## 三、推荐的下一步工作计划

按「业务价值 × 实施成本」评估，建议分三批推进：

### 第一批（立刻可启动，1-2 天）

1. **补提交第十期遗留在工作区的变更**（`build-version.js` 自动化 + `LAST_UPDATE` 时间戳）— 是个小收尾，避免脏工作区
2. **POS 收款 → 文创库存自动扣减** — 文创产品刚上线（第九期），库存联动缺失是最大的体验断点
3. **退款/作废功能** — 收银场景必备，当前没有会让录入数据「只能新增不能纠正」

### 第二批（1 周内，2-3 天）

4. **画廊销售独立 Tab** — 数据库表已建（`gallery_sales`），但前端无入口，录入挂靠在其他 Tab 下，体验差（场地租金 Tab 已在第一批中验证完成，无需重复）
5. **日结/交班报表** — 当日统计条已具备，扩展为可打印的日结单

### 第三批（持续，1 周+）

6. **ui.js 拆分**（2471 → 多文件）— 解决最大技术债
7. **数据库每日自动备份** — 加 `pg_dump` + cron，备份上传到腾讯云 COS
8. **HTTPS + API 认证中间件** — 安全长期方案

---

## 四、关键文件位置

| 类别 | 路径 |
|------|------|
| 项目根 | `D:\工作文档\00_进行中\艾维美术馆\00_工作台\运营数据管理\` |
| 开发日志 | `POS收银台开发日志.md`（10 期迭代记录） |
| PRD | `艾维美术馆运营数据管理系统-PRD.md`（v1.3，文件被截断需要补完） |
| 开发计划 | `艾维美术馆运营数据管理系统-开发计划.md`（最后更新 2026-07-04） |
| 部署方案 | `艾维美术馆运营数据管理系统-部署方案.md` |
| 前端代码 | `app/`（HTML/CSS/JS，无框架） |
| 后端 API | `server.js`（Node.js REST） |
| 数据库 | `app/sql/init.sql` |
| 版本号 | `VERSION` 文件（1.1.0）+ `app/js/app.js` APP_VERSION 常量 |

---

## 五、待用户决定的问题

1. **工作区有未提交变更**（+177 -18，含 build-version.js 自动化、版本号修复收尾），是否要先提交？
2. **下一步要启动哪个任务**？建议先做 **「POS 收款 → 文创库存自动扣减」**（业务最急迫、实施清晰）。
3. 上面三批计划的顺序是否调整？

---

## 六、报告后续

本报告生成于 2026-07-07 的对话中，作为后续工作推进的基线参考。后续每完成一个任务，应回看本报告更新进度。
