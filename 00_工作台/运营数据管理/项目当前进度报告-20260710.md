# 「运营数据管理」项目当前进度报告

**报告日期：** 2026-07-10
**基于：** 上一份报告 [项目当前进度报告-20260707.md](./项目当前进度报告-20260707.md) + 截至 2026-07-10 的代码现状 + 工作区未提交变更
**项目位置：** `00_工作台/运营数据管理/`

---

## 一、与上一份报告的变化（增量更新）

过去 3 天（7-7 → 7-10）的实际进展：

| 变化项 | 类型 | 说明 |
|---|---|---|
| 第十一期「空间页待收款卡片 + 校验 + 版本号/更新时间」 | 🆕 **未提交**（工作区脏）| 工作区里 +177 -18 变更全部属于这一期 |
| P0 复核 | 🔄 重核 | 文创扣减 / 退款作废 / 采购入库 / 日结报表 — 仍然全部未做 |
| 画廊销售独立 Tab | ✅ 已确认完成（再次验证）| 与上一份报告结论一致 |
| 开发计划文档 | ⚠️ 已过期 | 上次更新 2026-07-04，ui.js 1,220 → 2,520 行未回填 |
| 开发日志第十期标题 | ⚠️ 工作区冲突 | 工作区未提交了一份「2026-07-07 第十期」100 行内容，是新一期的命名错位（详见第三节） |

---

## 二、整体进展（截止 2026-07-10）

### 已完成（11 期迭代）

| 阶段 | 状态 |
|------|------|
| MVP 第一期：POS 收银机 | ✅ 完成 |
| MVP 第二期：产品动态管理 | ✅ 完成 |
| 用户登录系统（三角色 admin/editor/viewer） | ✅ 完成 |
| 文创产品管理模块（CRUD + 表格导入 + 库存联动） | ✅ 完成 |
| 腾讯云 Docker 部署（Nginx + Node.js API + PostgreSQL 17） | ✅ 完成 |
| 数据从 Supabase 迁移至腾讯云（175+ 条 revenue） | ✅ 完成 |
| Git 版本管理 | ✅ 完成 |
| 操作日志审计 | ✅ 完成 |
| 第十期（已提交）：套票 bug 修复 + 顶部实时统计扩展 + 趋势图「其他」分类 + 版本号修正 | ✅ 完成 |
| 第十一期（**未提交**）：空间页顶部待收款卡片 + 已收/应收校验 + 侧边栏更新时间 + build-version 自动化注入 APP_VERSION + server.js STATIC_DIR 环境变量 + CLAUDE.md 六步流程细化 + dist/ 镜像同步 | 🟡 **代码完成，未提交、未部署** |

### 当前线上状态

- **访问地址**：http://122.51.56.50（线上版本仍是第十期）
- **版本**：v1.1.0
- **数据库**：revenue 175+ 条（7-7 后无新增活跃数据）
- **git 工作区**：脏，`+177 -18` 全部为第十一期内容

---

## 三、第十一期内容明细（未提交的工作区变更）

### 3.1 功能改动

| 改动 | 文件 | 行 | 说明 |
|---|---|---|---|
| 空间页顶部待收款汇总卡片 | `app/js/ui.js` | +15（`_loadSpaceRentSummary`）| 比收银台文字提示更醒目（黄底+红边）；聚合 `rentalType==='付费'` 中 `receivableAmount > receivedAmount` 的记录 |
| 空间页调用时机 | `app/js/ui.js` | +1 | `renderSpacePage` 末尾追加 `await this._loadSpaceRentSummary()` |
| 已收/应收金额实时校验 | `app/js/ui.js` | +20（`_bindSpaceAmountValidation`）| 输入框红边 + `#fff5f5` 背景；超限时禁用保存 |
| 保存前校验 | `app/js/ui.js` | +8 | 已收 > 应收 时 toast 报错 + 输入框 focus |
| 编辑回填后自动校验一次 | `app/js/ui.js` | +6 | 处理历史数据已存在的「超收」情况 |
| 输入框 `.invalid` 样式 | `app/css/style.css` | +10 | 全局红边/红底/focus 阴影 |
| 空间页卡片样式 | `app/css/style.css` | +25 | `.space-rent-summary` 黄底+红左边框+深红文字 |
| 侧边栏更新时间显示 | `app/js/app.js` + `app/index.html` + `app/css/style.css` | +3 / +1 / +7 | 新增 `#sidebar-updated` 元素，硬编码 `LAST_UPDATE = '2026-07-07 16:30'`（注意：日期戳还是旧的，下次更新前要改）|
| 缓存标记 token 升级 | `app/index.html` | 4 处 | `space-rent-card-v2-20260707` / `space-rent-card-20260707` |
| `app.js` APP_VERSION 写入 | `app/index.html` | +1 | 硬编码 `v1.1.0`，配合 build-version.js 注入 |
| STATIC_DIR 环境变量化 | `server.js` | +1 | `process.env.STATIC_DIR || '/var/www/aiwei'` |
| build-version.js 增强 | `scripts/build-version.js` | +20 | 除注入 `index.html` 的 `?v=` 外，新增向 `app.js` 注入 `APP_VERSION` 常量；找不到常量时警告不报错 |

### 3.2 ⚠️ 已知问题（提交前必须处理）

1. **开发日志标题重复/错位**：`POS收银台开发日志.md` 工作区 diff 显示顶部新增了 100 行 `## 2026-07-07 第十期：录入 bug 修复...`，但 7-7 那期已经提交（commit `c39a174` ~ `0d95810`）。这是上一期对话**日志应该写但没写**的内容，被这一期对话**作为新一期的命名错误地补到了日志顶部**。
   - 建议：删除这次新增的 100 行，写一份新的 `## 2026-07-10 第十一期：...` 章节
2. **`LAST_UPDATE = '2026-07-07 16:30'`**：与本报告日期 7-10 不一致；build-version.js 已支持注入该常量，但只替换 `APP_VERSION` 不替换 `LAST_UPDATE`，需要手动同步或扩展脚本
3. **CLAUDE.md 和根 `dist/`** 也被修改了（7-7 报告之后的 3 天里追加了六步流程细化 + dist 镜像同步），提交前快速 `git diff ../../CLAUDE.md ../../dist/` 看一眼 — 已在 7-10 复核中确认：根 `CLAUDE.md` 新增「loop 模式 + 验证失败回落规则 + 核心强调」三段；根 `dist/` 与 `00_工作台/运营数据管理/app/` 内容一致（开发预览路径镜像），提交时一并带过去即可

### 3.3 部署状态

| 项 | 状态 |
|---|---|
| 代码 | 工作区未提交 |
| 服务器（122.51.56.50）| **未部署**，线上仍是第十期版本 |
| 验证（本地浏览器）| 未做 |
| 单元/E2E 测试 | 无（项目目前没有自动化测试）|

> **建议**：第十一期相对独立（不涉及数据库 schema 变更，纯前端样式 + 校验逻辑），但**部署前至少要在本地 preview 跑一遍**空间页，确认：
> 1. 顶部待收款卡片只在有未收款记录时显示
> 2. 已收/应收输入超限时，输入框变红、toast 报错、保存被拦截
> 3. 编辑历史数据时（已收 > 应收），输入框自动标红

---

## 四、待办事项（汇总自开发日志 + 代码复核）

### A. P0 立即可做（业务急迫）

2026-07-10 全量复核结论（grep + 读关键函数）：

| 任务 | 状态 | 代码证据 |
|---|---|---|
| ~~画廊销售独立录入 Tab~~ | ✅ **已完成** | `ui.js:1346 renderGalleryPage` + 侧边栏导航 + DB `gallery_sales` + 状态字段含「已退款」（注意：「已退款」是画廊销售的状态字段，不是 POS 收入表的退款功能）|
| ~~空间租金独立录入 Tab~~ | ✅ **已完成** | 「🏛 空间使用」Tab + `space_usage` 含 `rental_type` / `receivable_amount` / `received_amount` + 第十一期新增的待收款汇总卡片 |
| POS 收款 → 文创库存自动扣减 | 📋 **未完成** | `ui.js:366-375` 选产品时只过滤 `(p.stock||0) > 0`；`models.js:195` stock 只在产品 CRUD 写入；收款确认路径无 `stock -=` 逻辑 |
| 退款/作废功能 | 📋 **未完成** | `ui.js` 全文件无 `refund` / `void`；revenue 表无 `status` 字段；当前只有「删除」按钮 |
| 采购入库记录 | 📋 **未完成** | 全仓无 `purchase` / `stockIn` / `inbound` / `进货批次`；当前 stock 只能「直接改」，无入库历史 |
| 日结/交班报表 | 📋 **未完成** | 全仓无 `日结` / `交班` / `settlement` |

**真实 P0 = 4 项**：文创库存扣减 / 退款作废 / 采购入库 / 日结报表。

> ⚠️ **与上一份报告的差异**：上一份报告（7-7）也得出 4 项真实 P0 的相同结论。本轮复核未发现新完成项；唯一新发现是画廊销售的状态字段含「已退款」，但这不等于「POS 收入退款」功能实现。

### B. P1 增强功能

| 任务 | 说明 |
|---|---|
| 收银交易记录支持打印小票 | POS 标配 |
| 历史数据对比分析（同比/环比）| 数据洞察 |
| 库存预警（低于安全库存提示）| 配套采购 |
| 供应商管理模块（联系方式/账期）| 配套采购 |
| 细粒度操作日志审计 | 已做基础，可深化 |

### C. 技术债（持续累积）

| 风险 | 变化 | 说明 |
|---|---|---|
| `ui.js` 2,520 行单文件 | ⬆️ 比 7-4 报告多 1,300 行 | 拆分 POS / 管理 / 统计模块 |
| 无 HTTPS | 未变 | HTTP 环境 `crypto.subtle` 不可用 |
| API 无认证中间件 | 未变 | 可被直接调用 |
| 无数据库自动备份 | 未变 | 仅手动 `pg_dump` |
| 版本号硬编码 | 🆕 部分解决 | build-version.js 已能注入 `APP_VERSION`，但 `LAST_UPDATE` 仍需手动改 |
| 开发计划文档已过期 | 🆕 报告期 | 上次更新 2026-07-04，代码量与已完成项已严重失同步 |

---

## 五、推荐的下一步工作计划

### 第一批（立刻可启动，半天）

1. **处理第十一期工作区脏内容**
   - 重新整理 `POS收银台开发日志.md`，把误放在 7-7 那期的 100 行内容移到正确的「2026-07-10 第十一期」标题下，并补充「build-version 自动化 + 侧边栏更新时间 + STATIC_DIR 环境变量」三条记录
   - 同步 `LAST_UPDATE = '2026-07-10'`（或扩展 build-version.js 同时替换 `LAST_UPDATE`）
   - 提交这一期（commit message: `feat: 空间页待收款卡片 + 已收/应收校验 + 版本号自动化`）
   - 部署到腾讯云，线上刷新一次缓存

### 第二批（1 周内，2-3 天）

2. **POS 收款 → 文创库存自动扣减**（原 P0 第一项）
   - 后端：在 `POST /revenue` 后根据 `retailItems` 列表对 `creative_products.stock` 做 `-= qty`
   - 前端：扣减失败（如库存不足）回滚事务 + toast 提示
   - 数据库层：考虑加 `stock_movements` 表记录扣减流水
3. **退款/作废功能**
   - 数据库：revenue 表加 `status` 字段（`active` / `refunded` / `voided`）+ `refunded_at` / `refunded_by`
   - 前端：列表行加「退款」按钮，弹窗确认后修改状态
   - 联动：若原订单扣过文创库存，退款时反向回滚 stock

### 第三批（持续，1 周+）

4. **采购入库记录**（配套文创库存）
5. **日结/交班报表**
6. **ui.js 拆分**（2,520 行 → POS / 管理 / 统计 三个文件）
7. **数据库每日自动备份 + HTTPS + API 认证中间件**

### 文档同步

- **艾维美术馆运营数据管理系统-开发计划.md** 已过期（最后更新 2026-07-04，ui.js 行数从 1,220 → 2,520），建议下次启动任务前先回填到当前状态
- 本报告应取代上一份 7-7 报告，作为后续推进基线

---

## 六、关键文件位置

| 类别 | 路径 |
|---|---|
| 项目根 | `D:\工作文档\00_进行中\艾维美术馆\00_工作台\运营数据管理\` |
| 开发日志 | `POS收银台开发日志.md`（11 期迭代记录，工作区未提交）|
| 当前进度报告（旧）| `项目当前进度报告-20260707.md` |
| 当前进度报告（本份）| `项目当前进度报告-20260710.md`（本文件）|
| PRD | `艾维美术馆运营数据管理系统-PRD.md`（v1.3，文件被截断需要补完）|
| 开发计划 | `艾维美术馆运营数据管理系统-开发计划.md`（**过期**，2026-07-04）|
| 部署方案 | `艾维美术馆运营数据管理系统-部署方案.md` |
| 前端代码 | `app/`（HTML/CSS/JS，无框架）|
| 后端 API | `server.js`（Node.js REST）|
| 数据库 | `app/sql/init.sql` |
| 版本号 | `VERSION` 文件（1.1.0）+ `app/js/app.js` APP_VERSION 常量（build-version.js 自动注入）|

---

## 七、待用户决定的问题

1. **第十一期未提交变更**是否现在提交+部署？（建议：先在本地 preview 跑一遍空间页，确认无误后再处理）
2. **开发日志标题错位**（7-7 那 100 行内容）如何处理？
   - 选项 A：删除这次误放的内容，写一份新的 7-10 第十一期章节
   - 选项 B：把那 100 行重新组织成「2026-07-10 第十一期」+ 增补 build-version / 更新时间 / STATIC_DIR 三条
3. **下一步要启动哪个任务**？
   - 建议先收尾第十一期（提交 + 部署），再做 POS 文创库存扣减（业务最急迫）
4. **开发计划文档**要不要顺手回填？ui.js 行数已翻倍。

---

## 八、报告后续

本报告生成于 2026-07-10，作为后续工作推进的基线参考，**取代** 7-7 的旧报告。后续每完成一个任务，应回看本报告更新进度。