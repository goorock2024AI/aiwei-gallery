# 艾维运营数据库备份与恢复方案

更新时间：2026-07-24  
状态：v0.2，已配置云端每日自动备份、本地每日拉取备份，并完成首次隔离恢复演练；连续 7 日观察待补。

## 目标

为腾讯云 Docker PostgreSQL 经营数据库建立可验证备份，降低误删、接口滥用、服务器故障造成的数据不可恢复风险。

## 当前策略

| 项 | 设置 |
|---|---|
| 执行脚本 | `scripts/backup-db.sh` |
| 定时安装脚本 | `scripts/install-backup-cron.sh` |
| 服务器应用目录 | `/opt/aiwei` |
| 备份目录 | `/opt/aiwei/backups/postgres` |
| 日志目录 | `/opt/aiwei/backups/logs` |
| 默认时间 | 每日 03:17 |
| 默认保留 | 14 天 |
| 备份格式 | `pg_dump -Fc` custom format |
| 校验 | 生成 `sha256`，并执行 `pg_restore --list` 结构校验 |
| 首次手动备份 | 2026-07-24 12:42，生成 `aiwei-postgres-20260724-124229.dump` |
| 首次恢复演练 | 2026-07-24 12:43，临时容器恢复通过，表数 14，`revenue` 记录数 387 |
| 本地拉取脚本 | `scripts/local-backup-db.ps1` |
| 本地定时任务安装脚本 | `scripts/install-local-backup-task.ps1` |
| 本地备份目录 | `00_工作台/运营数据管理/local-backups/postgres` |
| 本地日志目录 | `00_工作台/运营数据管理/local-backups/logs` |
| 本地默认时间 | 每日 13:30 |
| 本地默认保留 | 10 天 |

脚本从 `/opt/aiwei/.env` 读取数据库环境变量，不在备份脚本中写入数据库口令。

## 手动备份

```bash
cd /opt/aiwei
bash scripts/backup-db.sh
```

完成后应看到：

- `backups/postgres/aiwei-postgres-YYYYMMDD-HHMMSS.dump`
- 同名 `.sha256`
- 同名 `.manifest`
- `backups/logs/backup-YYYYMMDD-HHMMSS.log`

## 安装定时任务

云端定时任务：

```bash
cd /opt/aiwei
bash scripts/install-backup-cron.sh
crontab -l | grep AIWEI_DB_BACKUP
```

如需调整时间：

```bash
AIWEI_BACKUP_CRON_SCHEDULE="17 3 * * *" bash scripts/install-backup-cron.sh
```

本地 Windows 定时任务：

```powershell
PowerShell -ExecutionPolicy Bypass -File "00_工作台\运营数据管理\scripts\install-local-backup-task.ps1"
schtasks /Query /TN AiweiLocalDbBackup
```

本地任务会先触发云端 `backup-db.sh` 生成最新备份，再把 `.dump`、`.manifest` 拉到本机，并在本地生成 `.sha256` 校验文件。本地只保留最近 10 天。

## 只读校验

云端：

```bash
cd /opt/aiwei
ls -lh backups/postgres
sha256sum -c backups/postgres/*.sha256
docker compose exec -T db pg_restore --list < backups/postgres/<backup-file>.dump >/dev/null
```

本地：

```powershell
PowerShell -ExecutionPolicy Bypass -File "00_工作台\运营数据管理\scripts\local-backup-db.ps1"
Get-ChildItem "00_工作台\运营数据管理\local-backups\postgres"
Get-FileHash "00_工作台\运营数据管理\local-backups\postgres\<backup-file>.dump" -Algorithm SHA256
```

## 恢复演练建议

正式恢复到生产库属于红线动作，必须由馆长或授权人确认。恢复演练应优先在隔离数据库或临时容器中执行：

```bash
docker run --rm --name aiwei-restore-test -e POSTGRES_PASSWORD=test -d postgres:17-alpine
sleep 5
cat backups/postgres/<backup-file>.dump | docker exec -i aiwei-restore-test pg_restore -U postgres -d postgres --clean --if-exists
docker exec -i aiwei-restore-test psql -U postgres -tAc "select count(*) from pg_tables where schemaname='public';"
docker rm -f aiwei-restore-test
```

## 当前服务器证据

- 定时任务：`17 3 * * * ... # AIWEI_DB_BACKUP`
- 备份目录：`/opt/aiwei/backups/postgres`
- 日志目录：`/opt/aiwei/backups/logs`
- 恢复演练日志：`/opt/aiwei/backups/logs/restore-drills.log`
- 本地任务：`AiweiLocalDbBackup`，每日 13:30
- 本地保留：10 天

## 未关闭风险

- 尚未完成 7 日连续备份观察。
- 当前自动任务需等 2026-07-25 03:17 后确认首次定时运行结果。
- 本地自动任务需等下一次 13:30 后确认定时运行结果。
