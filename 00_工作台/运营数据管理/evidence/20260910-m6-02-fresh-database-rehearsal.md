# M6-02 完整迁移链与新库重放证据

- 日期：2026-09-10
- 版本：`2.0.0-dev.m6-02.1`
- 范围：本机隔离 PostgreSQL；未连接生产、未部署、未读取真实业务数据
- 结论：通过，可进入 M6-03 代码、权限与核心流程回归

## 1. 可复现输入

1. 从提交 `bd309ee902901670a29965bb899cf0860f2e42e0` 固化 2.0 迁移前初始化脚本为 `sql/baselines/20260903-pre-v2-init.sql`。
2. 历史 Git blob 为 `9416985c057feef267ab471cf0b381c6f2564515`，基线文件 SHA-256 为 `4a659e770a702f833aa8a1c0066f171be5de268db9d9120b8dda701fef3294c2`。
3. `sql/m6-forward-manifest.json` 按固定顺序记录每个 SQL 的 SHA-256；本次清单 SHA-256 为 `a0864b87b699bdf378ba47ff27fa600d0ba7c973403b48f15355879faabbc783`。
4. 正向链共 18 个文件：1 个 1.0 商品/作品审批字段前置迁移，加 M3-02 至 M4-09 的 17 个结构、事实和治理迁移。逆向链共 14 个文件，按 M4-09 至 M3-02 的依赖逆序执行。

首次演练发现历史基线没有 `creative_products.approval_status`，而 M4-04 会读取该字段。该字段已有独立迁移 `20260827_role_permissions_product_approval.sql`，因此将其纳入清单前置步骤；没有把当前 `init.sql` 当作历史基线掩盖依赖缺口。

## 2. 演练流程

执行命令：`npm run check:m6-migrations`。

执行器只接受 `127.0.0.1`、`localhost` 或本机 IPv6 地址，并要求临时库名以 `aiwei_m6_` 开头。每次运行从 `template0` 新建数据库，拒绝复用已有目标库，完成后默认删除。

本次按以下顺序完成：

1. 应用历史 1.0 基线并写入确定性的旧版商品、收入、支出、画廊销售、空间项目、空间回款和日结样本。
2. 顺序应用 18 个正向 SQL；每个 SQL 在其正确前置状态下立即再执行一次，验证自身幂等。
3. 验证完整 2.0 结构、视图、字典和旧版事实数据。
4. 依赖逆序执行 14 个回滚 SQL，确认 2.0 视图和 M3 映射表关闭，同时保留 M4 批次审计表。
5. 从回滚态再次顺序应用完整 18 个正向 SQL，确认最终结构指纹与首次正向结果一致。

## 3. 验证结果

| 检查 | 结果 |
|---|---|
| 正向清单 | 18/18，通过 |
| 文件级幂等重放 | 18/18，通过 |
| 逆向回滚 | 14/14，通过 |
| 回滚后完整正向恢复 | 18/18，通过 |
| 业务维度 | 28 条：4 个业务层、11 个业务类型、10 个成本类型、3 个能力轴 |
| 必要核心表 | 7/7 |
| 必要 2.0 视图 | 18/18 |
| 最终结构指纹 | `054c84a998c1e1822e27fd954dd313fe`，首次正向与回滚后恢复一致 |
| 旧版事实 | 正向、幂等、回滚、恢复四个节点均逐字段不变 |
| 收入口径样本 | 原始总额 57.25 元完整保留；已归类 50.00 元；待归类 7.25 元进入治理问题 |
| 执行器记录的 SQL 耗时 | 848 ms |
| 临时数据库 | `aiwei_m6_02_20260910092133_6792`，结束后删除；查询残留数为 0 |

## 4. 回滚边界

- M3-04 没有独立回滚脚本；其视图替换最终由 M3-02 回滚关闭。
- M3-06 回滚先恢复前一版收入事实视图，随后 M3-02 回滚删除 2.0 核心视图和映射表。
- M4-09 的批次、项目和事件表属于审计记录，回滚只关闭汇总视图并保留审计表；演练明确验证三张表仍存在。
- 前置的 1.0 审批字段迁移不属于 2.0 功能回滚范围，因此不会在 M3/M4 逆向链中删除。

## 5. 准入结论

G0 的迁移清单、校验和、新库正向重放、文件级幂等、逆向回滚、回滚后恢复、结构一致性和旧版数据保护均有可复现证据。M6-02 完成，下一任务为 M6-03。生产备份、生产迁移和线上只读校验仍属于 M6-08 的单独授权范围。
