# M6-08 生产追加迁移与只读校验证据

日期：2026-09-11
生产版本：`2.0.0-dev.m6-08.1`
Git 提交：`b018193678caebfae2b1521f13c4f4d80bacc26b`
范围：生产备份、隔离恢复、M3–M6 追加迁移、API/静态部署和只读校验；2.0 灰度保持 `off`

## 生产恢复点

| 项 | 结果 |
|---|---|
| 备份文件 | `/opt/aiwei/backups/postgres/aiwei-postgres-20260911-133805.dump` |
| 大小 | 167,023 bytes |
| dump SHA-256 | `68d0dc5866b03b2cb65e4972d68b012bdbe75bc7d4cb0d88da1341cb8f0ccd79` |
| 基线 SHA-256 | `c621f9a02ec544be610df981e7bda3df5b0762a58e606e828ff63c7b5cca6a4d` |
| 远端校验 | dump SHA、基线 SHA、`pg_restore --list` 全部通过 |
| 本地隔离恢复 | PostgreSQL 17，388 ms；结构、记录数、金额和日期与生产备份点一致 |
| 临时恢复库 | 已删除，残留 0 |
| 发布前回退包 | `/opt/aiwei/backups/releases/pre-m6-08-20260911-134753.tar.gz` |

云端 `03:17` 定时任务仍存在，2026-09-04 至 2026-09-11 连续 8 天均有定时 dump。新版备份脚本和基线 SQL 已随本次版本部署，后续备份将生成 dump/baseline 双 SHA 和 manifest。

## 迁移执行与生产兼容修正

首次执行在 M4-08 自动停止。生产 1.3.3 的 `space_usage` 保留历史 `received_amount` 列，而全新测试基线没有该列；M4-08 的 `s.*` 与收款聚合产生同名字段。修正后使用内部名 `payment_received_amount`，对外视图列仍为 `received_amount`，未删除或覆盖历史列。

修正版先在生产备份的本地恢复库完整重放 20 个正向迁移，核心基线不变、28 个维度、9 张必要表、19 个必要视图、灰度 `off` 和 P0/P1 为 0。生产上的一次全链重试又在 M3-09 被 PostgreSQL阻止：早期视图定义不能覆盖已经被后续 M4 扩展的列。该尝试未提交破坏性变化；随后按已完成位置从 M4-08 恢复，M4-08、M4-09、M6-05、M6-06 全部通过。

三个服务器日志保留失败、重试和成功恢复过程：

- `/opt/aiwei/backups/logs/m6-08-migration-20260911-134010.log`
- `/opt/aiwei/backups/logs/m6-08-migration-retry-20260911-134246.log`
- `/opt/aiwei/backups/logs/m6-08-migration-resume-20260911-134316.log`

迁移后生产从 19 表/2 视图扩展到 28 表/21 视图，结构指纹为 `bca6698779cd5b87c430c06a71b466ea`。备份前后的核心记录数、金额和最新事实日期逐项一致，入口为 `off`，未复验 P0/P1 为 0。

## 部署与只读验证

发布包 SHA-256 为 `1fead044978fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`，保留生产既有 `8081:80` 映射和外层 HTTPS 代理。容器健康探针固定为 `127.0.0.1:3000/healthz`，避免 Alpine 中 `localhost` 解析导致误报。

- 数据库与 API 容器健康，API 返回 `{"status":"ok"}`。
- `https://iwe.ucanart.cc/` 返回 200，首页为 M6-08 缓存标识。
- 未登录读取 `/rest/v1/revenue` 返回 401。
- 19 个 2.0 视图全部可查询；本地四角色 100 项权限回归已通过，生产未创建测试账号或测试数据。
- 1.0 收入加 3 笔退款调整与 2.0 收入精确相等；期间支出相等。
- 销售成本、画廊、空间余额和日结公式不一致数均为 0。
- 发现 1 个历史空间项目实收大于应收；视图按冻结公式把待收置为 0，列为 P2 数据复核项，不阻断 G2。

`npm audit --omit=dev` 报告直接依赖 `xlsx` 有 1 个 high 且无自动修复版本。该依赖为既有能力，列为 P2 依赖替换/隔离评估项；当前无 critical，2.0 入口仍关闭。

## 结论

G2 通过：生产迁移只追加，核心事实不变，API 和 1.0 入口正常，权限入口保持关闭。可以准备 M6-09 内部灰度；开启 `admin` 或 `staff`、创建生产试运行问题/治理批次仍需下一任务单独授权。
