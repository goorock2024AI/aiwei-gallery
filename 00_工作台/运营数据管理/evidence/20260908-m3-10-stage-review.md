# M3-10 M3 验证与阶段评审

- 日期：2026-09-08
- 版本：`2.0.0-dev.m3-10.1`
- 分支：`codex/operations-v2`
- 范围：本地隔离 PostgreSQL 与 API，未连接生产环境

## 结论

M3 本地开发与验证有条件通过。核心业务流程、角色权限、日结兼容、v2 事实与治理视图、迁移回退边界都已达到进入 M4/M5/M6 本地设计与开发的条件。该结论不代表生产上线准入。

## 本轮关闭的问题

| 级别 | 问题 | 处理结果 |
|---|---|---|
| P1 | 旧通用 CRUD 完成后直接 POST `operation_logs`，请求不带认证并产生 401 | 改用 `/rest/v1/operation-log`，强制 admin/editor 登录 |
| P1 | 如果放开通用日志表写入，客户端可伪造 `user_id` | 专用端点忽略客户端身份，按 token 中的登录用户写入 |
| P2 | dist 同步检查漏了 `operation-logger.js` | 已加入 `check:dist-sync` 文件对 |

## 验证结果

| 类别 | 结果 |
|---|---|
| M3-05 支出归属 | 30 项 HTTP/SQL 检查通过 |
| M3-06 工坊项目 | 19 项 HTTP/SQL 检查通过 |
| M3-07 画廊交易 | 11 项 HTTP/SQL 检查通过 |
| M3-08 空间项目 | 14 项 HTTP/SQL 检查通过 |
| M3-09 管理汇总 | 11 项 HTTP/SQL 检查通过 |
| M3 迁移 | M3-04 快照与 M3-05 至 M3-09 回滚/重应用/幂等全部通过 |
| 1.0 日结 | 仍取 `revenue_facts`；本地日结金额 20.00，保存前后 1.0/v2 事实不变 |
| v2 边界 | 8 张视图 editor 可读、admin 写入均 405、viewer 管理汇总 403 |
| 审计 | 未登录 401、viewer 403、editor 201；伪造身份被覆盖 |
| 1.0 回归 | 商品快照、文创选择器、收入对比图、app/dist 同步通过 |

## 准入建议与未关闭项

1. 允许开始 M4/M5/M6 本地设计与开发。
2. 生产迁移、真实数据对账、历史回填、部署与试运行仍按 M2-07 单独授权和留证。
3. 上线前必须对治理清单中的历史待归类、缺成本商品/作品做真实数据处理。
4. Node `url.parse()` 弃用提示作为技术债进入后续维护，当前不阻断 M3 准入。

## 回退边界

- 本次无新增数据库迁移；回退服务端和静态文件即可撤销审计端点变更。
- M3-05 至 M3-09 各自的 rollback SQL 已验证保留原始收入、支出、画廊、空间、商品和日结数据。
- 本轮测试数据已清理，本地 API 已停止。
