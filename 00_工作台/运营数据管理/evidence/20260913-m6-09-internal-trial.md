# M6-09 内部灰度试运行与修正证据

日期：2026-09-13

生产版本：`2.0.0-dev.m6-08.1`

范围：备份前置复核、生产问题闭环、`off → admin → staff` 内部灰度、权限冒烟、真实数据对账和运行观测

最终范围：`staff` 内部灰度；admin/editor 开放，viewer 排除

## 备份阻断与恢复

灰度前检查发现云端 2026-09-12、09-13 的 03:17 定时任务均记录 `Permission denied`，原因是 Windows 发布组包未保留 Shell 可执行位。最近连续自动备份因此停在 09-11。入口在修复期间保持 `off`，问题以 P1 登记。

生产恢复 `scripts/*.sh` 的 755 权限；`deploy.sh` 增加远端权限校正，`deploy.sh`、`backup-db.sh`、`install-backup-cron.sh` 同时改为 Git 100755。修正后完成以下恢复点：

| 项 | 结果 |
|---|---|
| dump | `/opt/aiwei/backups/postgres/aiwei-postgres-20260913-120648.dump` |
| 大小 | 377,113 bytes |
| dump SHA-256 | `cd7710c5b1b0cc4fa70b4e976f2916be302298200abc43aa79caa5abe2c50883` |
| baseline SHA-256 | `d738de57e7de1ec4e3573bc4631ef88f9d07acd2223d1898090fa7fccacaf728` |
| 远端检查 | dump SHA、baseline SHA、`pg_restore --list` 通过 |
| 本地隔离恢复 | PostgreSQL 17，519 ms；基线一致，临时库 0 |

P1 经 `create → fix → verify` 三事件闭环，最终 `verified`，开放 P0/P1 为 0。下一次 09-14 03:17 自动备份仍须复核，当前结论不把手动备份视为自动连续性恢复。

## 灰度轨迹与权限

| 阶段 | UTC 时间 | 北京时间 | 审计原因 | 结果 |
|---|---|---|---|---|
| `off → admin` | 04:10:01 | 12:10:01 | `M6-09_admin_internal_trial` | 仅 admin 开放 |
| `admin → staff` | 04:17:51 | 12:17:51 | `M6-09_staff_internal_trial_after_admin_checks` | admin/editor 开放，viewer 关闭 |

两次变更均通过生产专用灰度接口执行并写入可信 `operation_logs`。服务器本地生成短时签名令牌用于自动化检查，令牌和密钥未离开容器、未写入证据。

每个检查点均对 admin/editor 读取 18 个受保护视图，共 36 次读取；viewer 读取敏感汇总返回 403，读取 1.0 收入返回 200。管理员阶段在 12:12、12:17 两轮检查无失败；员工阶段在 12:18、12:28 两轮检查无失败。分阶段观察从 12:10:01 持续至 12:28:21，员工窗口超过 10 分钟。

## 真实数据对账

| 项 | 结果 |
|---|---|
| 收入 | 1.0 `194,387.60` + 3 笔退款调整 `-68.00` = 2.0 `194,319.60`，差异 `0.00` |
| 期间支出 | 1.0 与 2.0 均为 `4,461.80`，差异 `0.00` |
| 销售成本 | 479 行，`1,897.29`；仅统计 `sold_cogs`/`gallery_settlement`，不并入期间支出 |
| 画廊 | 实现净额、退款、结算成本和贡献公式不一致 0 |
| 空间 | 待收 `GREATEST(0, 应收-实收)` 公式不一致 0；历史超收 1 行列为 P2 |
| 日结 | 已保存的系统净额、确认额、差异额内部公式不一致 0 |

两个已确认日结日期的当前收入比日结快照分别多 1,000.00 元和 33.00 元，说明闭账后存在同日追加流水；日结自身差异仍为 0，且 2.0 没有改写 1.0 日结。该现象列为 P2 复核闭账后操作规则。

## 观测和问题

管理员阶段结束前运行窗口累计 400 个请求，服务端错误 0，P50 8.4 ms、P95 100.1 ms、最大 1,321.7 ms。员工阶段结束时累计 494 个请求，服务端错误仍为 0，P50 8.0 ms、P95 227.2 ms、最大仍为 1,321.7 ms。两次历史 `business_layer_summary_v2` 请求超过 1 秒；阶段冒烟的 P95 为 0.85–0.87 秒，没有 5xx 或营业影响，列为 P2 持续观察。

生产试运行台账：

- P1 1 个：定时备份脚本执行权限中断，已复验关闭。
- P2 4 个：历史空间超收、`xlsx` high 依赖告警、经营汇总秒级慢请求、已确认日结后同日新增收入；均设负责人和截止时间。
- P0 0 个，未复验 P0/P1 0 个。

## 结论

预定的分阶段技术观察完成：每个阶段至少两次角色/入口检查，并完成敏感视图冒烟、真实金额对账、运行错误和问题门槛核对；全程无未关闭 P0/P1、无 5xx，对账无未解释的 2.0 金额差异。G3 通过，生产保留 `staff` 内部灰度，viewer 继续排除。

M6-10 仍须核对 09-14 首次自动备份、开放 P2 和实际业务验收，再由馆长决定是否正式切换；M6-09 不自动宣布 2.0 为正式入口。
