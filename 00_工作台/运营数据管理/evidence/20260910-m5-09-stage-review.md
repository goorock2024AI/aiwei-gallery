# M5-09 M5 阶段评审

- 日期：2026-09-10
- 版本：`2.0.0-dev.m5-09.1`
- 分支：`codex/operations-v2`
- 环境：本地隔离 PostgreSQL `127.0.0.1:55435/aiwei_m3_05_test`、本地 API `127.0.0.1:3127`、Microsoft Edge
- 边界：未连接生产、未执行真实历史治理、未部署

## 评审结论

M5 本地开发与验证有条件通过。M5-02 至 M5-08 已形成独立运营管理入口、四层经营总览、月度趋势与 1.0/2.0 差异、治理工作台、业务模型映射、来源下钻与导出、分区失败隔离和查询优化的完整本地闭环。允许进入 M6-01 验证、试运行与切换的任务拆分和本地方案冻结。

这一结论不代表生产发布准入。真实数据对账、历史治理批次、备份恢复演练、线上试运行、入口切换和云端部署仍需在 M6 按独立授权和留证执行。

## 验收结果

| 验收项 | 结果 | 证据 |
|---|---|---|
| 指标和口径 | 通过 | 16 条隔离汇总行逐项验证净收入、销售成本、期间成本、总成本、销售毛利、毛利率和经营贡献公式；零收入毛利率保持空值 |
| 四层与待归类 | 通过 | 四层顺序固定；Edge 隔离样本精确对账净收入 200、销售成本 30、期间成本 20、销售毛利 170、经营贡献 150、待归类影响 25 |
| 1.0/2.0 差异 | 通过 | 12 个月份、跨层合计和“2.0 减 1.0”差额的模型与 Edge 验收通过 |
| 治理对账 | 通过 | `data_governance_baseline_v2` 与问题明细聚合的 4 个隔离分组一致；六个候选域和批次状态 Edge 验收通过 |
| 业务模型 | 通过 | 28 个字典项为 4 业务层、11 业务类型、10 成本类型、3 能力轴，顺序和父级归属通过 |
| 筛选、下钻和导出 | 通过 | 年月、业务层、事实类型、映射状态和关键词通过；来源表、来源 ID、明细键和 UTF-8 CSV 通过 |
| 权限与只读 | 通过 | 66 项阶段 HTTP 检查覆盖 15 个敏感视图：editor 可读、viewer 403、未登录 401、admin 对视图写入 405；前端入口仅 admin/editor 可见 |
| 韧性和性能 | 通过 | 请求取消、过期响应保护、同范围旧数据保留、单区故障隔离、`count=none`、缓存和强制刷新通过 |
| 桌面和手机 | 通过 | M5-03 至 M5-08 六组 Edge 专项均覆盖 1440px 与 390px，页面异常和非预期控制台错误为 0 |
| M3/M4 回归 | 通过 | M3 管理汇总 11 项 HTTP；M4 批次流程 27 项 HTTP，包含审批、应用、撤销、并发保护和可信审计 |
| 原始事实 | 通过 | 阶段检查前后收入、支出、画廊、空间、收款和日结的行数及核心金额完全一致 |

## 自动化记录

- `npm run check:m5-stage-review`：66 项 HTTP/权限检查、指标公式、治理对账、字典数量、原始事实快照。
- `check:m5-overview-browser` 至 `check:m5-resilience-browser`：六组 Edge 实际页面回归通过。
- `check:m5-overview`、`check:m5-trend`、`check:m5-governance`、`check:m5-model`、`check:m5-detail`、`check:m5-resilience`：六组纯模型回归通过。
- `check:management-summary`：M3 四层管理汇总和权限回归通过。
- `check:m4-batches`：M4 受控批次完整流程回归通过。
- POS 商品选择、1.0 收入对比图、JavaScript 语法和 `app/dist` 同步检查通过。

## 评审中的环境修正

专用测试库初始只应用到 M4-05，因此五个后续治理视图请求返回 500。评审中按仓库已提交的 M4-06 至 M4-09 SQL 补齐该隔离库后，15 个敏感视图全部通过权限和只读验证。这是本地测试环境的迁移链漂移，不是 M5 页面口径缺陷。M6 必须将“从完整迁移链重建新库”列为准入检查，不再依赖长期复用的测试库状态。

## M6 准入与未关闭项

1. 允许开始 M6-01，冻结代码检查、数据库迁移检查、1.0/2.0 对账、灰度开关、试运行记录和回退条件。
2. M6 必须先使用新建隔离库完整重放 M2 至 M4 迁移，再开始线上或真实数据验证。
3. RLS 仍关闭，权限依赖 Node API；M6 需继续作为 P0 边界验证。
4. 生产备份、恢复演练、真实数据对账、历史治理、部署、灰度入口和正式切换都未开始。

## 下一任务

M6-01 验证、试运行与切换任务拆分与冻结。
