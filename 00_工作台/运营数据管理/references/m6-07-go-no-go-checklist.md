# M6-07 Go/No-Go 清单

## G1 本地前置门槛

- [x] 候选版本为 `2.0.0-dev.m6-07.1`。
- [x] 发布依赖完整，`app`/`dist` 同步。
- [x] 20 个正向迁移和 16 个回滚文件存在且规范化 LF 后 SHA-256 匹配。
- [x] 本地 PostgreSQL 17 custom dump 可列举对象。
- [x] 同一备份连续恢复两次，结构、记录数、金额和日期基线一致。
- [x] 第一次恢复后的探针写入未出现在第二次恢复中。
- [x] 源测试库在演练前后不变，临时恢复库已删除。
- [x] 灰度为 `off`，未复验 P0/P1 为 0。
- [x] 运行手册、停止条件、代码回退点和整库恢复边界已记录。
- [x] `/healthz` 实际连接数据库并返回 200；容器编排以该端点判断 API 健康。

结论：**G1 通过，可准备 M6-08；不构成生产操作授权。**

## M6-08 执行前必填

- [ ] M6-08 生产读取、备份、迁移和部署已单独授权。
- [ ] 变更单、执行窗口、生产负责人、数据库操作人、复核人已填写。
- [ ] 候选 Git 提交和上一生产提交/镜像已锁定。
- [ ] 当前生产备份已生成，dump/baseline 双 SHA 和对象清单通过。
- [ ] 当前生产备份已在隔离环境恢复并与生产备份点基线一致。
- [ ] 当前生产灰度为 `off`，未复验 P0/P1 为 0。
- [ ] 1.0 核心流程基线和生产回退路径已验证。

任一项未完成即 No-Go。
